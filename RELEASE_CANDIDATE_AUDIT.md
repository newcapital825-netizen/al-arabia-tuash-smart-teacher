# Release Candidate Audit

## نطاق التدقيق

أُجري هذا التدقيق على النسخة الحالية من **«المعلم الذكي الصغير — أحمد صلاح | العربية تُعاش»** باعتبارها Release Candidate، مع الالتزام بعدم إضافة خصائص أو تطبيق إصلاحات تلقائية أثناء التدقيق. استُخدمت قراءة الشفرة، اختبارات Vitest الموجودة، سجلات الشبكة والخادم، المعاينة المرئية، ومحاولة رفع فعلية ظهرت في السجلات.

## A — PASS

| الوظيفة | النتيجة | الدليل المختصر |
|---|---|---|
| حماية مسارات الخادم بالمصادقة | PASS | مسارات التفعيل والرفع والأسئلة محمية بـ `protectedProcedure`، ومسارات المالك بـ `adminProcedure`. اختبارات tRPC الحالية تؤكد رفض المستخدم غير الإداري لمسارات المالك. |
| عزل وثيقة المستخدم في الاستعلام | PASS | `getDocument(id, userId)` يفرض تطابق مالك الوثيقة مع المستخدم الطالب قبل الإجابة. |
| عدم وضع مفاتيح الخادم في واجهة العميل | PASS | الفحص النصي لملفات `client/src` لم يجد `DATABASE_URL` أو `JWT_SECRET` أو `BUILT_IN_FORGE_API_KEY`. |
| تفعيل الترخيص من Backend | PASS جزئيًا | القرار يُنفذ في الخادم ويُسجل في جدول `licenses`، وليس مجرد شرط واجهة. هذا يثبت نقطة Backend لكنه لا يثبت اكتمال الحماية التجارية. |
| رفض الترخيص المعطل في المسار | PASS | `license.activate` يتحقق من الحالة المعطلة ويرجع `FORBIDDEN` ويسجل المحاولة. |
| منع غير المالك من لوحة المالك | PASS | `adminProcedure` يتحقق من `ctx.user.role`; الاختبار التكاملي أكد رفض المستخدم العادي. |
| عدم حفظ النص المستخرج في أعمدة قاعدة البيانات | PASS | جدول `documents` يحتفظ ببيانات وصفية و`analysisKey` فقط، وليس `extractedText` أو `summary`. |
| منطق مؤشر التقدم بعد 55% | PASS على مستوى الوحدة | اختبار `uploadProgress.test.ts` يثبت انتقال 55 إلى 56 وحد 92 ثم الصفر بعد الفشل. هذا ليس اختبار متصفح كاملًا، لذلك لا يكفي وحده لإثبات تجربة الإنتاج. |
| فحص TypeScript والاختبارات الآلية | PASS كفحص آلي | `pnpm check` نجح، ونجحت 21 حالة اختبار في 7 ملفات. لا يُعد ذلك بديلًا عن الاختبارات الفعلية المطلوبة في المتصفح. |

## B — PARTIAL

| الوظيفة | النتيجة | السبب |
|---|---|---|
| المصدر الواحد والإجابة المقيدة | PARTIAL | يوجد Prompt وتحقق من الاستشهادات في الخادم، لكن لم يكتمل اختبار متصفح فعلي بالأسئلة الخمسة المطلوبة على ملف مرفوع حقيقي. كما أن النص المرسل للنموذج يُقص إلى 60,000 حرف، فلا يمكن اعتبار الاستيعاب الكامل مضمونًا للملفات الأكبر. |
| الاستشهاد | PARTIAL | الاستشهادات الحالية أرقام فقرات اصطناعية ناتجة من تقسيم الأسطر، وليست أرقام صفحات PDF أو مواضع أصلية قابلة للتحقق دائمًا. التحقق البرمجي يمنع الاستشهاد غير الموجود، لكنه لا يثبت أن الفقرة تمثل الموضع الأصلي في الملف. |
| OCR العربي | PARTIAL | يوجد اتصال متعدد الوسائط إلى `gemini-3-flash-preview`، وتُبنى الاستشهادات من النص الناتج، لكن لم تُنفذ مقارنة فعلية لنص عربي مطبوع أو مكتوب بخط اليد مع النص المستخرج. لا توجد نسبة دقة مقاسة. |
| التلخيص | PARTIAL | التعليمات تقيد النموذج بالمصدر، لكن لم تُسجل نتيجة تلخيص فعلية من ملف الاختبار المرجعي، ولا يمكن إثبات عدم إسقاط معلومة جوهرية من دون مقارنة بشرية منظمة. |
| الترخيص وربط الجهاز | PARTIAL | الترخيص يُربط بحساب وبـ`boundDeviceHash` عند أول تفعيل، لكن القرار اللاحق يتحقق من المستخدم/البريد ولا يقارن `deviceHash` بالحقل المخزن؛ لذلك لا يثبت منع نفس الحساب من جهاز آخر. |
| الخصوصية | PARTIAL | النص لا يدخل قاعدة البيانات، لكنه يُخزن في JSON داخل التخزين مع `analysisKey` ولا توجد سياسة مدة احتفاظ أو حذف أو انتهاء جلسة محددة. |
| تصدير PDF العربي | PARTIAL | يوجد توليد PDF فعلي بـ`jsPDF` ويُضاف الاسم والهوية، لكن لم يُختبر ملف PDF العربي فعليًا من حيث اتصال الحروف وRTL والخط والاقتباسات. الخط الافتراضي في jsPDF لا يثبت دعم العربية الإنتاجي. |
| الرفع والصيغ | PARTIAL | قائمة الصيغ موجودة، واختبار tRPC mock لملف TXT ينجح، لكن لم ينجح رفع فعلي كامل من الواجهة للأنواع المطلوبة. |
| الاتصال البطيء | PARTIAL | يوجد مؤشر وتقدم دوري ومهلة 90 ثانية، لكن لم يُنفذ اختبار شبكة 3G فعلي يثبت عدم التكرار أو السلوك تحت تباطؤ حقيقي. |
| لوحة المالك والإحصاءات | PARTIAL | توجد إحصاءات التراخيص والرفع وتعطيل الترخيص، لكن لم تُختبر دورة كاملة من الواجهة تشمل التعطيل ثم توقف الوظائف المدفوعة. |

## C — FAIL

| الوظيفة | النتيجة | الدليل المختصر |
|---|---|---|
| رفع ملف عربي فعلي باسم عربي | FAIL | سجل الشبكة الفعلي يُظهر طلب `documents.upload` بملف PDF ذي اسم عربي، ثم `Storage presign failed (400): invalid argument, file path must be ASCII` وتحول الطلب إلى HTTP 500. ظهر الانتظار حتى نحو 89.7 ثانية في محاولة، ونحو 12.5 ثانية في محاولة أخرى. هذا يطابق شكوى التوقف عند 55%. |
| منع تسريب محتوى الملف في السجلات | FAIL | `networkRequests.log` يحتوي جسم طلب `documents.upload` مع `base64` لملف PDF، أي أن محتوى الملف يُسجل في سجل الشبكة. هذا يخالف شرط عدم تسجيل محتوى الملفات بصورة غير مقصودة. |
| منع تسريب رمز الجلسة في سجلات الشبكة | FAIL من منظور التدقيق | سجل الشبكة يحتوي ترويسة Authorization بتوكن جلسة. حتى إن كان السجل أداة تصحيح داخلية، لا توجد سياسة موثقة تمنع الاحتفاظ به أو تعرضه، ويجب اعتباره خطرًا قبل النشر. |
| ربط الجهاز فعليًا | FAIL بالنسبة للمتطلب الصريح | `boundDeviceHash` يُخزن عند التفعيل، لكنه لا يُستخدم في `evaluateLicense` عند التفعيل اللاحق؛ الحساب نفسه يستطيع التفعيل من جهاز آخر ما دام الحساب مطابقًا. |
| عداد المحاولات المدفوع | FAIL بالنسبة للمتطلب | النظام يسجل أحداث `upload`, `summary`, `question` ويعرض عدد الرفع، لكنه لا يفرض حد محاولات أو رصيدًا أو نفاد محاولات. لذلك لا يمكن اختبار حماية عداد قابل للتجاوز لأنه غير منفذ كحصّة مدفوعة. |

## D — NOT TESTABLE

| البند | النتيجة | السبب |
|---|---|---|
| PDF نصي كامل من البداية للنهاية | NOT TESTABLE | المحاولة الفعلية المتاحة فشلت قبل التحليل بسبب اسم الملف العربي في التخزين. |
| PDF مصور، DOCX، TXT، JPG، PNG | NOT TESTABLE فعليًا | لا يوجد في السجلات دليل على رفع كل نوع من الواجهة وتحليل نتيجته. اختبار TXT الموجود mock وليس طلبًا حقيقيًا. |
| صورة عربية مطبوعة ومقارنة OCR | NOT TESTABLE | لم تُقدم عينة صورة عربية موثقة ولم تُسجل مقارنة النص الأصلي بالناتج. |
| صورة عربية بخط اليد | NOT TESTABLE | لا يوجد اختبار فعلي أو ادعاء مدعوم بأن هذا السيناريو يعمل. |
| الأسئلة الخمسة على مصدر واحد | NOT TESTABLE | لم تُنفذ جلسة متصفح كاملة تشمل سؤالًا مباشرًا، موزعًا، خارج المصدر، معلومة عامة، وPrompt Injection. |
| فتح Incognito وجهاز آخر وSafari/Edge/Firefox/iPhone/Android | NOT TESTABLE | البيئة الحالية لا توفر جلسات وأجهزة فعلية مستقلة كافية، ولم يتم تنفيذ هذه السيناريوهات. |
| تعطيل ترخيص من الواجهة ثم إعادة محاولة الرفع | NOT TESTABLE | توجد اختبارات مسارات mocked، لكن لا توجد دورة متصفح حقيقية موثقة بعد التعطيل. |
| تصدير PDF العربي المرئي | NOT TESTABLE | لم يتم تنزيل PDF فعلي من جلسة متصفح وتحليل مخرجه بصريًا أو بنيويًا. |
| انقطاع اتصال وفشل Gemini وصورة غير واضحة وملف تالف | NOT TESTABLE | لم تُنفذ محاكاة فعلية لهذه الحالات في المتصفح. |

## E — Security Risks

الخطر الأبرز هو أن حماية الجهاز غير مكتملة: يتم حفظ بصمة الجهاز، لكن لا تُستخدم في قرار السماح اللاحق. كما أن قيمة `INTERNAL_TEST_KEY` الثابتة في الشفرة هي المفتاح القديم `ARABIA-INTERNAL-TEST-2026`، بينما المفتاح التجريبي الحالي الذي أُنشئ في قاعدة البيانات هو `ARABIA-INTERNAL-TEST-2026-B`; لذلك لا تمنع قاعدة إصدار التراخيص المفتاح الحالي B من الإصدار التجاري إذا أُدخل من لوحة المالك.

التحقق من دور المالك يعتمد على قيمة `role` في قاعدة البيانات، وهو Backend gate مناسب مبدئيًا، لكن لم يُختبر تغيير الدور أو إساءة استخدام جلسة مالك مسروقة. لم تُظهر واجهة العميل مفاتيح الخادم في الفحص النصي، لكن يجب التعامل مع سجلات الشبكة التي تحمل Authorization كبيانات حساسة.

## F — Privacy Risks

ما يتم تخزينه هو بيانات المستخدم الأساسية في `users`، وبيانات الترخيص ومحاولاته في `licenses` و`licenseAttempts`، وبيانات وصفية للوثيقة في `documents`، وأحداث الاستخدام في `usageEvents`. النص المستخرج والملخص يُحفظان داخل JSON في التخزين الخارجي، ويُشار إليهما من قاعدة البيانات عبر `analysisKey`. الملف الأصلي نفسه يُرفع إلى التخزين الخارجي عبر `student-files/...`.

لا توجد في الكود سياسة مدة احتفاظ أو وظيفة حذف تلقائي للملف الأصلي أو JSON التحليل. كما أن سجل الشبكة الحالي احتفظ بجسم base64 للملف وبتوكن Authorization، وهذا خطر خصوصية مباشر. لا توجد أدلة على ظهور النص في لوحة المالك، لكن لا يمكن اعتبار الخصوصية مكتملة ما دامت السجلات والاحتفاظ غير محددين.

## G — External Services

| الخدمة | الوظيفة | الحساب/المفتاح | ملاحظات التدقيق |
|---|---|---|---|
| Manus OAuth | تسجيل الدخول والجلسات | يعتمد على إعدادات المشروع | ظهر في المعاينة تحويل إلى صفحة تسجيل الدخول. لم يُختبر تدفق العودة كاملًا لهذا التدقيق. |
| Manus Forge Storage/S3 presigned URLs | تخزين الملفات والتحليلات | مفتاح الخادم `BUILT_IN_FORGE_API_KEY` | الخدمة مطلوبة للتخزين. السجل الفعلي رفض أسماء الملفات غير ASCII. حدود السعر والاحتفاظ غير محددة في المشروع. |
| Manus Forge LLM / Gemini model | التلخيص والإجابة وOCR | مفتاح الخادم عبر `ENV.forgeApiKey` | يستدعى النموذج `gemini-3-flash-preview` عبر Backend. لم تُقَس التكلفة أو حدود الاستخدام من التطبيق. |
| Manus notification | تنبيه محاولات التفعيل المشبوهة | إعداد المشروع | لا يوجد دليل متصفح على وصول بريد فعلي للمالك. |
| jsPDF | توليد PDF في المتصفح | لا يحتاج حسابًا خارجيًا | دعم العربية الإنتاجي غير مثبت بالتدقيق الفعلي. |
| Manus Analytics | التحليلات | Endpoint وهوية الموقع من الإعدادات | أُرسلت أحداث زيارة في السجل؛ سياسة البيانات والاحتفاظ خارج نطاق المشروع غير موثقة هنا. |

لا يمكن الجزم من المشروع وحده بأن الخدمات مجانية أو مدفوعة أو ما هي حصصها الحالية؛ هذه معلومات حساب/خطة خارجية يجب مراجعتها في لوحات الخدمات قبل الإطلاق التجاري.

## H — Launch Readiness

### القرار

# NOT READY

السبب الحاسم هو فشل رفع ملف فعلي باسم عربي بسبب قيد ASCII في مسار التخزين، مع بقاء الطلب في حالة طويلة بدل رسالة مفهومة، إضافة إلى تسجيل base64 ومعلومات جلسة في سجل الشبكة، وعدم فرض ربط الجهاز وعدم وجود عداد محاولات مدفوع فعلي. كما أن معظم اختبارات PDF/DOCX/OCR/المتصفحات والتصدير العربي لم تُنفذ فعليًا.

وفقًا للتعليمات، لم أطبق أي إصلاح أثناء التدقيق. الخطوة التالية المطلوبة هي موافقتك على إصلاحات محددة، بدءًا من ترميز/تطبيع أسماء الملفات، منع تسجيل أجسام الطلبات والتوكنات، فرض بصمة الجهاز، وتحديد سياسة احتفاظ وحذف واضحة، ثم إعادة التدقيق الفعلي.
