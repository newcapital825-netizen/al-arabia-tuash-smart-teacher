# تقرير تنفيذ واختبار — المعلم الذكي الصغير

## الحالة الحالية

تم تنفيذ نسخة ويب أولية عربية RTL تعمل داخل مشروع كامل مع مصادقة، قاعدة بيانات، تخزين ملفات، تفعيل ترخيص مرتبط بالحساب، واجهة رفع، تلخيص، أسئلة وأجوبة، تصدير عبر نافذة الطباعة، ولوحة مالك مقيدة بدور `admin`. تظهر هوية **أحمد صلاح — العربية تُعاش** في شاشة الدخول، الواجهة الرئيسية، التذييل، ومسار التصدير.

## ما تم التحقق منه

| المجال | النتيجة الحالية | الدليل أو الملاحظة |
|---|---|---|
| TypeScript | ناجح | `pnpm check` اكتمل بلا أخطاء |
| Vitest | ناجح | اختباران ناجحان: المصادقة وسياسة الرفض الثابتة |
| واجهة سطح المكتب | تم التحقق بصريًا | لقطة شاشة 1280×720 |
| واجهة الهاتف | تم التحقق بصريًا | لقطة شاشة 390×844، RTL، شاشة التفعيل قابلة للاستخدام |
| الترخيص | منفذ | المفتاح يُربط بالحساب عند أول تفعيل، وتُرفض الحسابات الأخرى، ويُرسل إشعار للمالك |
| الخصوصية | محسّنة | قاعدة البيانات تحتفظ ببيانات وصفية ومرجع تخزين فقط؛ لا تحفظ النص المستخرج أو الملخص |
| OCR عربي | منفذ للصور | الصور تُرسل إلى نموذج Gemini متعدد الوسائط مع تعليمات استخراج عربية |
| الشبكة البطيئة | واجهة مؤشر مضافة | شريط تقدم ظاهر أثناء القراءة والإرسال، ويحتاج اختبار شبكة 3G خارجيًا للتأكيد |

## القيود التي يجب عدم تجاهلها

نسخة الإصدار الأول لا تُعد جاهزة للإطلاق التجاري النهائي بعد. مسار PDF وDOCX يحتاج استخراجًا نصيًا حقيقيًا صفحةً بصفحة؛ حاليًا يدعم الواجهة والرفع، بينما يعتمد التحليل غير النصي على رابط/وصف الملف بدل محلل PDF/DOCX مكتمل. كما أن الاستشهادات الموضعية الدقيقة ورقم الصفحة لم تُفرض بعد في بنية مخرجات منظمة، ولذلك يجب إكمالها قبل التسويق بالدقة الموعودة.

تصدير PDF الحالي يفتح نسخة HTML للطباعة من المتصفح، وليس ملف PDF مولّدًا خادميًا قابلًا للتنزيل بجودة إنتاجية ثابتة. كما أن الاختبارات الآلية الحالية لا تغطي بعد كل مسارات التفعيل والربط والرفض ولوحة المالك ومحاولات Jailbreak.

## الترخيص التجاري والخدمات الخارجية

البنية الحالية تدعم إصدار المفاتيح من لوحة المالك وتفعيلها داخليًا. ربط الدفع الفعلي بخدمة مثل Lemon Squeezy أو Gumroad أو Stripe يتطلب حسابًا وخدمة خارجية ومفاتيح سرية يملكها أحمد صلاح. لا ينبغي إصدار مفاتيح للعملاء من دون ربطها بعملية دفع أو إدخالها يدويًا من لوحة المالك.

## الخلاصة

النسخة الحالية مناسبة كنقطة انطلاق قابلة للعرض والاختبار الداخلي، وليست شهادة توافق نهائية مع Chrome وSafari وEdge وFirefox على كل أنظمة التشغيل. يلزم تنفيذ اختبارات أجهزة فعلية أو BrowserStack/مختبر مشابه، وإغلاق البنود المتبقية في `todo.md` قبل الإطلاق التجاري.

## مفتاح الاختبار الداخلي

مفتاح الاختبار الداخلي الحالي هو `ARABIA-INTERNAL-TEST-2026`. هذا المفتاح موسوم في قاعدة البيانات بالعلم `isInternalTest`، ويُظهر للمستخدم تنبيهًا صريحًا عند التفعيل بأنه مخصص للاختبار الداخلي فقط وليس للبيع أو التوزيع. يُستخدم بحساب اختبار واحد، وقد أصبح مرتبطًا بأول حساب يفعّله وفق سياسة ربط التراخيص. لا يُسلّم هذا المفتاح للعملاء ولا يُعد إثباتًا لشراء أو اشتراك تجاري.
