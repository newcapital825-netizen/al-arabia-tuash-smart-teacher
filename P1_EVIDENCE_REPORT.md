# P1 Evidence Report

## نطاق التنفيذ

نُفذت اختبارات P1 الممكنة من البيئة الحالية دون طلب إدخال المفتاح أو الموافقة على الشروط أو تشغيل اختبار متصفح ميداني. استُخدمت خدمة Gemini الحقيقية عبر النموذج `gemini-3-flash-preview`، ولم تُستخدم Mock لإثبات نتيجة النموذج.

## A — Storage

| Test | Result | Evidence |
|---|---|---|
| تحديد الكائنات التي ينشئها التطبيق | **PASS** | الملف الأصلي يحفظ عبر `storagePut` في مفتاح `student-files/...`، وJSON التحليل في مفتاح `student-analysis/...`؛ لا توجد كائنات مؤقتة أخرى في مسار الرفع الحالي. |
| حذف فعلي للكائنات بعد الاحتفاظ | **IMPLEMENTATION GAP** | `server/storage.ts` يوفر `storagePut` و`storageGet` و`storageGetSignedUrl` فقط. دليل التخزين الرسمي ينص على أن طبقة القالب لا تعرض endpoint حذف للكائن؛ لذلك لا يمكن إثبات حذف S3 الفعلي من هذه البيئة. |
| سياسة الاحتفاظ | **PASS جزئيًا** | التطبيق يحدد افتراضيًا 7 أيام، ويحجب الوثيقة المنتهية ويسقط مراجعها من قاعدة البيانات عبر مسار المالك. إسقاط المرجع ليس حذفًا للبايتات. |
| اختبار إنشاء/تخزين/انتهاء/حذف/التحقق من الغياب | **BLOCKED — STORAGE DELETE API REQUIRED** | لا يمكن تنفيذ الخطوة الأخيرة بصدق دون API حذف فعلي؛ لم يتم إنشاء كائن اختبار يتعذر تنظيفه. |

## B — Real Gemini/OCR

| Test | Result | Evidence |
|---|---|---|
| اتصال Gemini الحقيقي | **PASS** | استُخدم endpoint الحي و`gemini-3-flash-preview`، وعادت استجابة صحيحة دون Mock. |
| استخراج نص عربي من مادة نصية | **PASS** | أُرسل مصدر عربي عن دورة الماء، وأعاد النموذج جوابًا مطابقًا للمصدر. |
| سؤال إجابته داخل المصدر | **PASS** | عاد الجواب: «تبخر مياه البحار والأنهار بفعل حرارة الشمس» مع اقتباس حرفي من المصدر. |
| سؤال خارج المصدر | **PASS** | عاد الرد الثابت: «هذا السؤال غير موجود ضمن محتوى الملف المرفوع.» |
| OCR لصورة عربية فعلية | **PASS** | أُنشئت صورة PNG فعلية تحتوي نصًا عربيًا مطبوعًا، وأعاد Gemini النص العربي الظاهر فيها بدقة مطابقة للعينة. |
| المسار الكامل Input → OCR/Extraction → Analysis → Answer → Citation | **PASS جزئيًا** | نجحت أجزاء OCR/Extraction وAnalysis وAnswer في الاختبار الحي؛ الاستشهاد ظهر كاقتباس حرفي قابل للتحقق، لكن اختبار ملف PDF حقيقي مع ربط صفحة فعلية لا يزال محجوبًا باختبار الرفع الميداني. |

## C — Real Upload

| Test | Result | Evidence |
|---|---|---|
| PDF عربي باسم ملف عربي من المتصفح | **BROWSER-BLOCKED** | يتطلب اختيار ملف من جلسة متصفح مصادق عليها؛ لم تُستخدم Mock ولم يُطلب تدخل المستخدم في هذه الجولة. |
| TXT عربي باسم ملف عربي من المتصفح | **BROWSER-BLOCKED** | يتطلب اختيار ملف حقيقي في المتصفح المصادق عليه؛ اختبار Backend الموجود لا يثبت Storage حقيقيًا أو تجربة المتصفح. |
| DOCX/JPG/PNG من المتصفح | **BROWSER-BLOCKED** | يتطلب بيئة متصفح وجلسة رفع حقيقية؛ لا تُسجل النتيجة PASS من الاختبارات المموهة. |
| إصلاح `file path must be ASCII` | **PASS برمجيًا / NOT PROVEN ميدانيًا** | مفتاح التخزين الداخلي UUID/ASCII ومغطى باختبار تكامل؛ لم يُثبت بعد من متصفح بملف PDF عربي فعلي. |

## D — Citation

| Test | Result | Evidence |
|---|---|---|
| الإجابة موجودة في المصدر | **PASS** | جواب دورة الماء يتطابق مع جملة المصدر العربي حرفيًا. |
| الاستشهاد قابل للتحقق | **PASS** | الاقتباس المعاد من Gemini هو: «تبدأ دورة الماء بتبخر مياه البحار والأنهار بفعل حرارة الشمس.»، وهو موجود حرفيًا في العينة. |
| عدم اختلاق رقم صفحة | **PASS** | لا يستخدم الاختبار الحي رقم صفحة غير متاح؛ يستخدم اقتباسًا حرفيًا/موضعًا وصفيًا. |
| عرض الاستشهاد بعد استئناف الملف | **PASS برمجيًا** | `documents.access` يعيد رابط التحليل الموقّع، والواجهة تطبع `citations` بعد تطبيع الحمولة؛ اختبار الوحدة يرفض بنية الاستشهاد غير الصحيحة. |
| موضع صفحة PDF حقيقي | **NOT TESTABLE IN CURRENT ENVIRONMENT** | يحتاج PDF حقيقيًا مرفوعًا من جلسة قبول مع الحفاظ على mapping الصفحة أثناء الاستخراج. |

## E — Multipart Decision

**NOT NEEDED YET** للانتقال الفوري إلى multipart في هذه الجولة. السبب أن اختبار P1.3 الحقيقي لم يُنفذ بسبب حظر المتصفح، وبالتالي لا يوجد دليل جديد يثبت أن base64 فشل بعد إصلاح مفتاح التخزين. القرار مشروط: إذا أظهر اختبار القبول لاحقًا فشلًا مع الملفات الكبيرة، أو timeout، أو عدم استئناف، فستكون الخطوة التالية Architecture Plan منفصلًا قبل أي تنفيذ multipart.

## F — Remaining Gaps

الفجوة التنفيذية الأساسية هي عدم توفر API حذف فعلي لكائنات التخزين في طبقة القالب الحالية، ولذلك لا يمكن إثبات حذف S3 الفعلي. كما أن رفع PDF/TXT العربي من المتصفح المصادق، واختبار PDF/صفحات حقيقية، لم يُنفذ في هذه الجولة. جودة OCR/Gemini أثبتت بنجاح على العينة العربية الحية، لكنها لا تغطي بعد ملف PDF عربي كبير متعدد الصفحات.

## G — ONE-TIME HUMAN ACCEPTANCE TEST

هذه القائمة لا تطلب تنفيذها الآن، بل تجمع الحدود التي تحتاج جلسة قبول واحدة لاحقًا:

1. اختيار PDF عربي وTXT عربي من المتصفح المصادق باسم ملف عربي، ثم إثبات Storage → Extraction → Analysis → Result.
2. التحقق من ملف PDF متعدد الصفحات بأن موضع الاستشهاد لا يدّعي رقم صفحة غير محفوظ.
3. تشغيل الرفع على Safari/iPhone وAndroid وSafari Desktop وEdge وFirefox، مع اختبار صورة الكاميرا و3G.

## الحكم الحالي

**P1 PARTIAL — REAL GEMINI/OCR PASS، REAL UPLOAD BROWSER-BLOCKED، STORAGE DELETE IMPLEMENTATION GAP.** لم يبدأ P2، ولم يُطلب من المستخدم إدخال المفتاح أو الموافقة على الشروط.
