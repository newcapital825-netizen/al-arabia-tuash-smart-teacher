# Gate 1A Decision Package

## الغرض والحالة

هذه الحزمة تحوّل نتائج **Real Document Gauntlet** إلى قرار تنفيذي قابل للمراجعة. لا تُنفّذ هذه الوثيقة OCR redesign أو تغييرات في استخراج PDF أو R2 أو Gate 2؛ وظيفتها تحديد ما يجب إصلاحه وما الدليل المطلوب قبل إعادة فتح Gate 1A.

> القرار الحالي: **GATE 1A PARTIAL — SPECIFIC GAPS REMAIN**.

أثبتت العينة `DOCX-001` أن مسار DOCX العربي قادر على استخراج نص عربي مستقر، وبناء Canonical Representation، وإنشاء Evidence IDs قابلة لإعادة البناء. في المقابل، فشلت عينات PDF الثلاث في تمثيل الصفحات والطبقة العربية، بينما أنتجت الصورة العربية OCR قابلًا للقياس لكنه غير حتمي بين تشغيلين. لذلك لا يجوز إعلان Gate 1A ناجحًا أو الانتقال إلى RAG أو Embeddings أو Pilot.

## 1. الأدلة المثبتة

| المسار | النتيجة | الدليل التشغيلي | الأثر على القرار |
|---|---|---|---|
| DOCX عربي | **PASS / STABLE** | 2,233 محرفًا عربيًا، 120 Evidence، وثبات كامل في النص والبنية والمعرّفات عبر تشغيلين | يمكن الاحتفاظ بالمسار كخط أساس مرجعي |
| PDF عربي نصي مستقل | **FAIL** | 26 صفحة في metadata مقابل صفحة واحدة في Canonical، و459 وحدة بلا محارف عربية | لا يمكن استخدامه للإجابة أو الاستشهاد |
| PDF عربي مصوّر | **FAIL** | فقد الطبقة العربية والصفحات، مع عدم تشغيل OCR fallback | يحتاج كشف النوع وOCR على مستوى الصفحة |
| صورة عربية | **PARTIAL / NON-DETERMINISTIC** | 49 محرفًا عربيًا ومؤشرات عربية قابلة للقياس، لكن text/structure/Evidence اختلفت بين تشغيلين | لا يمكن اعتمادها كمصدر حتمي بعد |
| الفشل الآمن | **PASS جزئي** | `NO_EXTRACTABLE_TEXT` و`UNSUPPORTED_MIME_TYPE` ورفض PDF التالف | يحمي من تمرير نتائج فارغة، لكنه لا يعالج جودة PDF/OCR |

## 2. خيارات الإصلاح

| الخيار | الوصف | المزايا | المخاطر والقيود | التوصية |
|---|---|---|---|---|
| A — إصلاح محافظ داخل المسار الحالي | اكتشاف نوع PDF، ثم اختيار text extraction أو OCR fallback، مع تقسيم الصفحات قبل بناء Canonical | أقل تغييرًا، ويحافظ على DOCX وواجهات Evidence الحالية | قد تبقى جودة OCR غير حتمية، ويحتاج اختبارًا دقيقًا للصفحات | **الخيار الموصى به كبداية** |
| B — خدمة OCR متخصصة للوثائق | إرسال كل صفحة مصورة إلى محرك OCR عربي مخصص مع إحداثيات ونِسَب ثقة | أفضل احتمال لاستعادة page/box mapping | اعتماد خارجي، تكلفة، أسرار، ومتطلبات خصوصية واحتفاظ جديدة | يُدرس بعد إثبات حدود الخيار A |
| C — مسار مزدوج مع قبول عدم الحتمية | حفظ عدة مرشحين OCR أو استخدام consensus قبل بناء Evidence | قد يحسن التغطية للصورة | يزيد التعقيد، ولا يضمن Evidence حتميًا دون سياسة اختيار قابلة للتدقيق | غير مناسب قبل وجود Benchmark |
| D — تجاوز PDF والصورة مؤقتًا | قبول DOCX/TXT فقط في Pilot داخلي | يقلل المخاطر المباشرة | لا يحقق دعم الملفات المطلوب، ولا يحل Gate 1A | غير مقبول كقرار تجاري نهائي |

## 3. القرار الهندسي

يُعتمد **الخيار A** كمسار الإصلاح الأول فقط بعد موافقة منفصلة على التنفيذ. يجب أن يتكون من مراحل صغيرة قابلة للرجوع: اكتشاف PDF text-layer، استخراج كل صفحة مع `sourcePageNumber` حقيقي، تشغيل OCR fallback للصفحات التي لا تحتوي نصًا صالحًا، ثم بناء Canonical وEvidence IDs من كل صفحة مع حفظ provenance. لا يجوز تمرير نص غير عربي أو Canonical يضم صفحة واحدة لملف متعدد الصفحات على أنه مصدر صالح.

أما الصورة المنفردة فتظل محجوبة حتى تُعرّف سياسة حتمية قابلة للتدقيق: إما نتيجة OCR ثابتة، أو درجة ثقة/مقارنة نصية تمنع اعتماد Evidence إذا تغيرت النتيجة بين تشغيلين. لا يُسمح بإخفاء التذبذب داخل إجابة الطالب.

## 4. معايير إعادة فتح Gate 1A

لا يُعاد فتح Gate 1A إلا بعد تحقق الشروط التالية على عينات جديدة وغير موجودة في GitHub:

| المعيار | شرط الخروج |
|---|---|
| DOCX regression | استمرار PASS / STABLE لـ`DOCX-001` مع عدم كسر 36 اختبارًا قائمًا |
| PDF text-layer | تطابق عدد الصفحات في metadata مع `Page` nodes، ووجود محارف عربية عند توقعها، وLocation حقيقي لكل Evidence |
| PDF scanned | اكتشاف غياب text-layer، تشغيل OCR fallback لكل صفحة، وربط كل Evidence برقم الصفحة الصحيح؛ لا توجد صفحة واحدة بديلة لملف متعدد الصفحات |
| Image OCR | تشغيلان متتاليان ينتجان نصًا وبنية وEvidence IDs متطابقة، أو سياسة ثقة موثقة تمنع الاعتماد عند الاختلاف |
| Evidence reconstruction | إعادة بناء Evidence من البداية والمنتصف والنهاية إلى المصدر الصحيح، دون اقتباس غير موجود |
| Failure safety | المدخل الفارغ أو التالف أو غير المدعوم يفشل برسالة صريحة ولا يمر إلى الملخص أو QA |
| Privacy | لا نص خام أو صورة أو Base64 أو Secret في DB أو logs أو GitHub أو Manifest |
| Validation | `pnpm check` و`pnpm test` و`pnpm build` ناجحة بعد الإصلاح |

## 5. ما لا يُنفّذ الآن

لا تُنفّذ في هذه المرحلة أي تغييرات في OCR أو PDF parser، ولا تُنشأ خدمة OCR خارجية، ولا تُطلب أسرار، ولا يبدأ R2 أو Gate 1B أو Gate 2 أو RAG أو Embeddings أو Vector Search أو Payment أو Credits أو Pilot أو UI redesign أو Production deployment. كما لا تُرفع العينات الحقيقية إلى GitHub؛ GitHub يحتفظ بالكود والتقارير metadata-only فقط.

## 6. ترتيب التنفيذ عند الموافقة لاحقًا

يبدأ التنفيذ المستقبلي بفرع مستقل لـPDF type detection وpage-level extraction، ثم اختبارات PDF text/scanned، ثم OCR fallback، ثم إعادة بناء Evidence، ثم اختبار الصورة، ثم تشغيل الاختبارات الحالية والبناء، ثم تحديث Gate 1A Report. إذا فشل أي شرط، تبقى الحالة PARTIAL أو FAIL ولا يُسمح بالانتقال.

## 7. القرار النهائي

> **GATE 1A remains PARTIAL. DOCX is the only tested format currently PASS / STABLE. PDF and Image paths are not production-ready. Do not proceed to Gate 2.**

هذه الحزمة لا تمنح موافقة تنفيذ؛ إنها سجل قرار وحدود. التنفيذ التالي يحتاج موافقة صريحة على فتح مسار إصلاح PDF/OCR مستقل، مع بقاء R2 والخصوصية وGate 2 خارج النطاق.
