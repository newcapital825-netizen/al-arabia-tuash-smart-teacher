# تقرير P0 Regression Audit

## القرار

**الحالة: P0 IMPLEMENTED — NOT RELEASE-APPROVED YET.** تم تنفيذ إصلاحات P0 الحرجة فقط، ونجح الفحص الآلي والبناء، لكن لا أصف النسخة بأنها جاهزة للنشر التجاري قبل إجراء اختبار رفع حي من جلسة مستخدم فعلية واختبار متصفحين/جهازين مستقلين. توقفت عمدًا قبل أي بند P1.

## ما تم إصلاحه

| المجال | النتيجة | الدليل التنفيذي |
|---|---|---|
| مفتاح التخزين | **PASS** | أصبح اسم التخزين UUID/ASCII مع الاحتفاظ بالاسم العربي في `filename` للعرض والتصدير. يغطي الاختبار ملفًا اسمه `درس عربي.txt`. |
| تسريب السجلات | **PASS داخل جامع السجلات المرفق** | أصبحت أجسام طلبات `documents.upload` و`documents.ask` و`license.*` والترويسات الحساسة مستبدلة بعلامات Redacted، ولا تُلتقط استجاباتها النصية. |
| Device Binding | **PASS** | التفعيل الأول يحفظ بصمة الجهاز، والحساب أو الجهاز المختلف يُرفض من Backend، وأضيف `owner.rebindDevice` مع زر «إعادة ربط» في لوحة المالك واختبار Backend. |
| Usage Balance | **PASS برمجيًا** | الخطط متاحة: Free Trial = 3، Limited = 60، Open = غير محدود. الخصم ذري من Backend، ويُرجع عند فشل تقني في التحليل أو الإجابة. |
| الاحتفاظ | **PASS وظيفي / PARTIAL تخزينيًا** | تحفظ الوثائق والتحليلات لمدة افتراضية قدرها 7 أيام، وبعدها لا يعيد `getDocument` المحتوى. أضيف `owner.cleanupExpired` لإسقاط مراجع التخزين المنتهية، كما تحمل الوثائق حالة `documentStatus` صريحة (`active` أو `failed` أو `expired`) تمنع السجلات الفاشلة من الظهور في الاسترجاع. طبقة التخزين لا تعرض حذفًا فعليًا للكائن، لذلك لا أدعي حذف البايتات من S3. |
| الفشل المبكر | **PASS** | أضيفت مهلة 15 ثانية للتخزين و90 ثانية للتحليل/الإجابة ورسائل عربية واضحة، مع استرجاع محاولة الرصيد ووسم سجل الوثيقة `failed` وإسقاط مرجعه عند فشل خصم الرصيد أو التحليل أو التخزين التحليلي. تغطي ذلك اختبارات تكامل مستقلة. |
| الاختبارات | **PASS** | `pnpm check` نجح، و`pnpm test` نجح: 8 ملفات اختبار و29 اختبارًا. |
| بناء الإنتاج | **PASS** | `pnpm build` نجح وبُنيت حزمة الواجهة والخادم. |
| التحقق البصري | **PASS جزئيًا** | شاشة التفعيل RTL وهوية «أحمد صلاح — العربية تُعاش» ظهرت سليمة في معاينة سطح المكتب. أضيف استئناف الملف من `localStorage` ومسار وصول موقّع مع رابط «فتح الملف»، لكن لم يُنفذ رفع حي من حساب مستخدم داخل هذه الجولة. |

## مصفوفة اختبارات P0

| # | السيناريو | النتيجة | ملاحظة |
|---:|---|---|---|
| 1 | فتح التطبيق دون ترخيص | **PASS** | مسار الواجهة يبقى عند شاشة التفعيل. |
| 2 | تفعيل مفتاح صالح لأول مرة | **PASS** | يغطيه اختبار `license.activate` مع حفظ الحساب والجهاز. |
| 3 | إعادة التفعيل من حساب مختلف | **PASS** | يُرفض من Backend وتُسجل المحاولة ويُرسل إشعار المالك. |
| 4 | إعادة التفعيل من جهاز مختلف | **PASS** | يُرفض من Backend برسالة إعادة ربط مصرح بها. |
| 5 | رفع اسم ملف عربي | **PASS** | مفتاح التخزين ASCII والاسم الأصلي محفوظ للعرض. |
| 6 | محاولة رفع بلا ترخيص | **PASS** | تُرفض قبل التخزين. |
| 7 | محاولة رفع بعد نفاد الرصيد | **PASS** | تُرفض عند بلوغ الحد. |
| 8 | فشل التخزين أو مهلة النموذج | **PASS برمجيًا** | تظهر رسالة خطأ ولا يبقى الطلب عالقًا بلا نهاية، وتُسترجع المحاولة عند فشل التحليل/الإجابة. |
| 9 | سؤال مع جهاز غير مرتبط أو وثيقة غير متاحة | **PASS** | يُفرض الجهاز وتُحجب الوثيقة المنتهية أو غير المملوكة. |
| 10 | تنظيف الوثائق المنتهية | **PASS إداريًا / PARTIAL تخزينيًا** | مسار المالك يُسقط مراجع قاعدة البيانات، ويحجب `getDocument` المحتوى بعد الانتهاء؛ حذف كائن S3 الفعلي غير متاح في طبقة التخزين الحالية. |

## أداء الوصول والاستعادة

أصبح معرف آخر وثيقة محفوظًا محليًا، ويُستعاد عبر `documents.access` بعد إعادة فتح الصفحة مع تحقق الترخيص وبصمة الجهاز، ثم تُجلب حمولة التحليل الموقعة ويُعرض الملخص والاستشهادات. أضيفت خيارات `staleTime` ومنع إعادة الاستعلام عند تركيز النافذة لمسار الترخيص والوصول لتقليل التنقلات البطيئة.

في القياس البرمجي الحالي، استغرق اختبار مسار الاستعادة الموقّع أقل من 250ms ضمن الاختبار، واستغرق تشغيل مجموعة الاختبارات الكاملة 2559ms بجدار الزمن، مع 8 ملفات و29 اختبارًا ناجحًا. هذه أزمنة طبقة الاختبار وليست وعدًا بزمن شبكة الإنتاج؛ زمن التخزين وGemini الفعلي سيظل تابعًا للخدمة والاتصال.

## المخاطر المتبقية قبل الإطلاق

لم يُنفذ بعد اختبار حي كامل بملف PDF عربي كبير من جلسة مستخدم مسجّل الدخول، ولم يُتحقق فعليًا من Gemini/OCR العربي في هذه الجولة، كما لم يُقَس زمن الشبكة في Safari أو الهاتف. كما لم يُختبر Safari وFirefox وEdge وiOS وAndroid أو محاكاة 3G من خلال أجهزة حقيقية. هذه ليست ادعاءات نجاح؛ لذلك تبقى **PARTIAL أو NOT TESTABLE** حتى تُنفذ في بيئة قبول حقيقية.

كما أن جامع السجلات المنقح جزء من النسخة المرفقة، لكنه لا يغطي أي وكيل شبكي خارجي خارج الصفحة نفسها. ينبغي مراجعة إعدادات سجلات الاستضافة قبل الإطلاق التجاري للتأكد من عدم تسجيل أجسام الطلبات في طبقة خارجية.

> **نطاق التنفيذ:** تم تنفيذ P0 فقط. لم تُنفذ أي تحسينات P1 مثل رفع multipart، التخزين المؤقت، progress حقيقي من الخادم، التحقق المرئي الكامل، أو تكامل بوابة الدفع.

## خاتمة القرار

النسخة أصبحت **أكثر أمانًا من ناحية P0**، ويمكن الانتقال إلى اختبار قبول يدوي محدود، لكنها لا تحمل بعد قرار «جاهزة للنشر التجاري» بسبب القيود الميدانية المذكورة أعلاه. أي انتقال إلى P1 يجب أن يبدأ بعد موافقة صريحة على هذا التقرير.
